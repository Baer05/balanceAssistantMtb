pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Add local libs repo
        // If do not add this, when use our SDK, it will show ERROR: Failed to resolve: :XsensDotSdk:
        flatDir {
            dirs "libs"
        }

    }
}
rootProject.name = "balanceAssistantMtb"
include ':app'
